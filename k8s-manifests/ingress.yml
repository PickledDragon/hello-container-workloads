apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: container-demo
  annotations:
    kubernetes.io/ingress.class: addon-http-application-routing
spec:
  rules:
    - host: container-demo.a7ff6b7a5d17435091da.westeurope.aksapp.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: container-demo
                port:
                  number: 80
