apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: container-demo
  annotations:
    kubernetes.io/ingress.class: addon-http-application-routing
spec:
  rules:
    - host: container-demo.c511cb01728e441eb31c.westeurope.aksapp.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: container-demo
                port:
                  number: 80
